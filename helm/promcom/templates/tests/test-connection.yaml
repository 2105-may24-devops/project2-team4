apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "promcom.fullname" . }}-test-connection"
  labels:
    {{- include "promcom.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "promcom.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
